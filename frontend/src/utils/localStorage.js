const FAVORITES_KEY = "menuapp_favorites";

export const getStoredFavorites = () => {
  return [];
};

export const saveStoredFavorites = (favorites) => {};

export const addFavorite = (restId) => {};

export const removeFavorite = (restId) => {};
